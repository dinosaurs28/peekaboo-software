rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder: tighten later. See README for planned role-based rules.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
